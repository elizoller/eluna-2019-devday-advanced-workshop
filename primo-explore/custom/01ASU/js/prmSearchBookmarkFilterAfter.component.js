import { vid } from './viewId';

export let PrmSearchBookmarkFilterAfterConfig = {
  name: 'prmSearchBookmarkFilterAfter',
  config: {
    bindings: {},
    templateUrl: `custom/${vid}/html/prmSearchBookmarkFilterAfter.component.html`
  }
};